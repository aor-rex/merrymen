# Match the root package's resolver when installing the packed merrymen app.
# Privy's optional smart-wallet peer is deliberately unused; resolving its
# incompatible ox peer would prevent the desktop package from installing.
legacy-peer-deps=true
